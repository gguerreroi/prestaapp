<div id="kt_header" class="header align-items-stretch">
    <div class="container-fluid d-flex align-items-stretch justify-content-between">

        <!-- Mobile aside toggle -->
        <div class="d-flex align-items-center d-lg-none ms-n2 me-2">
            <div class="btn btn-icon btn-active-light-primary" id="kt_aside_mobile_toggle">
                <i class="bi bi-list fs-1"></i>
            </div>
        </div>

        <!-- Header title -->
        <div class="d-flex align-items-center flex-grow-1">
            <div class="page-title d-flex flex-column justify-content-center me-3">
                <h1 class="page-heading d-flex text-dark fw-bold fs-3 flex-column justify-content-center my-0">
                    Inicio
                </h1>
                <ul class="breadcrumb breadcrumb-separatorless fw-semibold fs-7 my-0 pt-1">
                    <li class="breadcrumb-item text-muted">Resumen de su operación hoy</li>
                </ul>
            </div>
        </div>

        <!-- Header right -->
        <div class="d-flex align-items-center flex-shrink-0">
            <!-- Search -->
            <div class="d-none d-md-flex align-items-center me-3">
                <span class="position-absolute ms-4" style="z-index:1;">
                  <i class="bi bi-search fs-4 text-gray-600"></i>
                </span>
                <input type="text"
                       class="form-control w-250px ps-12"
                       style="background-color: #e4e6ef;"
                       placeholder="Buscar cliente, préstamo, recibo..." />
            </div>

            <!-- User -->
            <div class="d-flex align-items-center">
                <div class="d-flex flex-column me-3 text-end">
    <span class="fw-bold text-gray-800">
      <%= user?.NombreCompleto || user?.strnombre || 'Usuario' %>
    </span>
                    <span class="fs-8 text-muted">
      <%= user?.roleName || (user?.codrole ? `Rol ${user.codrole}` : '') %>
    </span>
                </div>

                <div class="symbol symbol-35px symbol-circle">
                        <span class="symbol-label bg-light-primary text-primary fw-bolder">
        <%= (user?.strnombre?.[0] || user?.NombreCompleto?.[0] || 'U').toUpperCase() %>
      </span>
                </div>
            </div>
        </div>

    </div>
</div>